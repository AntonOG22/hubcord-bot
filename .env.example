DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DASHBOARD_SESSION_SECRET=
DASHBOARD_PORT=3000
PUBLIC_URL=

SUPABASE_URL=
SUPABASE_KEY=

# Optional: powers the in-dashboard AI agent (public/app.js chat widget).
# Leave blank to disable it — /api/ai/chat then returns 503 and the widget
# just shows an error if opened. Never exposed to the browser; read only
# server-side in dashboard.js.
MISTRAL_API_KEY=
